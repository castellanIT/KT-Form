version: 1
frontend:
  phases:
    preBuild:
      commands:
        - '[ -f package-lock.json ] || cd ../../..'
        - npm ci
    build:
      commands:
        - '[ -f package-lock.json ] || cd ../../..'
        - node scripts/generate-config.js
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
backend:
  phases:
    build:
      commands:
        - cd amplify/backend/function/makeProxy && npm install